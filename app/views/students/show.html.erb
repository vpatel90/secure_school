<h1><%= link_to "students", school_students_path(@school.id) %></h1>

<p>Find me in app/views/students/show.html.erb</p>
<div class="row">
  <%= link_to "Delete", school_student_path(school_id: @student.school_id,
              id: @student.id), method: "DELETE",
              class: "delete-edit-button right red waves-effect waves-light btn",
              data: { confirm: "Are you sure you want to delete this School?" }%>

    <%= link_to "Edit", edit_school_student_path(school_id: @student.school_id,
                               id: @student.id),
                class: "delete-edit-button right orange waves-effect waves-light btn"%>

  <div class="col s12 m8 l10">
      <h3> <%= @student.name %> </h3>
    </div>
</div>
