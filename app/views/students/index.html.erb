<h1>students#index</h1>
<p>Find me in app/views/students/index.html.erb</p>
<%= link_to "Add Student", new_school_student_path, class:"right blue darken-2 waves-effect waves-light btn" %>
<%= render partial: 'schools/schoolbox', locals: { school: @school } %>
<% @students.each do |student| %>
  <%= render partial: 'studentbox', locals: { student: student } %>
<% end %>
