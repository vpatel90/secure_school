<h1>courses#index</h1>
<p>Find me in app/views/courses/index.html.erb</p>
<%= link_to "Add course", new_school_course_path, class:"right blue darken-2 waves-effect waves-light btn" %>
<%= render partial: 'schools/schoolbox', locals: { school: @school } %>
<% @courses.each do |course| %>
  <%= render partial: 'coursebox', locals: { course: course } %>
<% end %>
